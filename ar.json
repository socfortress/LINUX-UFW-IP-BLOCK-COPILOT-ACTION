{
  "copilot_action_name": "LINUX-UFW-IP-BLOCK-COPILOT-ACTION",
  "description": "Blocks a specified IPv4 address using UFW by adding a deny rule. Checks if the IP is already blocked and logs a detailed status and reason. Writes a single NDJSON object atomically to the Active Response log with .new fallback if locked.",
  "technology": "Linux",
  "icon": "linux",
  "script_parameters": [
    {
      "name": "IP",
      "type": "string",
      "required": true,
      "description": "IPv4 address to block via UFW (e.g., 203.0.113.45).",
      "default": null
    },
    {
      "name": "ARLog",
      "type": "string",
      "required": false,
      "description": "Active Response NDJSON output path (atomic overwrite; falls back to .new if locked).",
      "default": "/var/ossec/active-response/active-responses.log"
    },
    {
      "name": "LogPath",
      "type": "string",
      "required": false,
      "description": "Path to the rotating human-readable script log.",
      "default": "/tmp/Block-IP.log"
    },
    {
      "name": "LogMaxKB",
      "type": "integer",
      "required": false,
      "description": "Maximum size of the log file (KB) before rotation.",
      "default": 100
    },
    {
      "name": "LogKeep",
      "type": "integer",
      "required": false,
      "description": "Number of rotated log files to keep.",
      "default": 5
    }
  ],
  "repo_url": "https://github.com/socfortress/LINUX-UFW-IP-BLOCK-COPILOT-ACTION",
  "script_name": "UFW-Block-Ip.sh",
  "version": "1.0.0",
  "last_updated": "2025-08-23T00:00:00Z"
}
